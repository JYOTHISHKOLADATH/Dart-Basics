import 'dart:io';
import 'dart:math';
void main(){
  try{
    stdout.write("Enter the radius");
    int r=int.parse(stdin.readLineSync()!);
    double area=3.14*pow(r, 2);
    print(e);
  }
  catch(e){
    print(e);
  }
}