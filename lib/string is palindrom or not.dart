import 'dart:io';
void main(){
  String word;
  int l,i,k;
  stdout.write("Enter the string");
  word=stdin.readLineSync()!;
  l=word.length;
  for(i=0;i<=l/2;i++){
    for(k=l;k>=l/2;k--){
      if(wor)
    }
  }
}